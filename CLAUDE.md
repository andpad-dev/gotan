# CLAUDE.md

このリポジトリは Go Conference 2026 のワークショップ「Go探無比 (gotan)」の教材リポジトリです。
問題の作成・修正を手伝うときは、以下の規約に従ってください。

## ファイル配置

- 問題は `<カテゴリ>/<難易度>/<問題名>/README.md` に置く。1 問 1 ディレクトリ。
- カテゴリは `01-packages` / `02-features` / `03-cmd-tools` / `04-deep-dive` のいずれか。
- 難易度ディレクトリは `01-beginner`（初級）/ `02-intermediate`（中級）/ `03-advanced`（上級）。
- `<問題名>` はテーマが分かる英語のケバブケース（例: `fmt-printf`, `slog-handler`）。連番は振らない。
- 解説用の別ファイル・別ディレクトリ（solutions 等）は作らない。ヒントと答えは問題の README に `<details>` で折りたたんで収録する。
- 各カテゴリのルートには調べ方をまとめた `README.md`（逆引き手順）を置く。問題を追加するカテゴリに無ければ作成を提案する。

## 難易度と出題フォーマット

難易度は配置ディレクトリと PR の**ラベル**（`初級` / `中級` / `上級`）の両方で表現し、必ず一致させる。

| 難易度 | ディレクトリ | 言い回し | 設問数 |
|:---|:---|:---|:---|
| 初級 | `01-beginner` | 「◯◯を見かけました。どんなものか調べてみましょう」 | 2 つほど |
| 中級 | `02-intermediate` | 「◯◯をやりたいです。どういうふうにやればいいか調べよう」 | 3 つ |
| 上級 | `03-advanced` | 「なんでこうなってるの？背景を調べよう」 | 制限なし |

例題: [初級 #2](https://github.com/andpad-dev/gotan/pull/2) / [中級 #3](https://github.com/andpad-dev/gotan/pull/3) / [上級 #4](https://github.com/andpad-dev/gotan/pull/4)

## 問題 README の構成

1. `# <タイトル>`
2. 導入 — 開発現場の困りごととして状況を説明し、難易度の言い回しで調査を促す。問題コードがある場合は Go Playground のリンクを添えて、すぐ動かせるようにする
3. `## 設問 N: <見出し>` を難易度の目安の数だけ。設問ごとに:
   - 問題文
   - 必要なら `<details><summary>ヒント</summary>` で折りたたんだヒント
   - `<details><summary>答え</summary>` で折りたたんだ解説（**調査ルート**（どの一次情報を・どの順で辿るか）と**答え**）
4. 任意で `<details><summary>こぼれ話</summary>` — 本筋から外れるが面白い発見
5. `## 調査の入り口` — 起点となる一次情報リンク

体裁のルール:

- 設問・こぼれ話・調査の入り口の各セクションの間は `---`（区切り線）で区切る。
- `<details>` の中では Markdown 見出し（`#`）を使わない。「調査ルート」「答え」のようなラベルは `**太字**` で書く。折りたたみを開いたときに見出しが大きく表示されて読みづらいため。

「まず `f` キーで検索する」のような、調査の最初のトリガーとなる共通手順はヒントに書いて隠さない。
カテゴリルートの README（逆引き手順）に載せる。

## 品質規約（必須）

- **リンクは実在を確認する。** 参照 URL はすべて `curl` 等で HTTP ステータスを確認してから書く。
- **コードは実行して確かめる。** スニペットは手元で `go run` し、出力を確認してから載せる。出力例は実際の出力をそのまま書く。
- **問題コード・実行例には Go Playground のリンクを付ける。** 共有リンクは次で作成できる:
  `curl -s -X POST --data-binary @main.go https://play.golang.org/share` → `https://go.dev/play/p/<返ってきたID>`
- **一次情報を根拠にする。** pkg.go.dev・go.dev/ref/spec・Go 本体ソース（cs.opensource.google）を辿り、記憶や二次情報の推測で断定しない。
- Go 本体ソースへのリンクはバージョンタグ付きで書く（例: `refs/tags/go1.26.5`）。

## PR 運用

- ブランチは `feature/scenario-<問題名>`、`origin/main` から切る。
- PR は Draft で作成し、配置ディレクトリの難易度と一致するラベルを 1 つ付ける。
- PR body はテンプレートに沿って書く。案出し時のメモや経緯は書かない。
- コミットメッセージは Conventional Commits（例: `feat: 中級問題「◯◯」を追加`）。
